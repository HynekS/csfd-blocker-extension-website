{
  "scripts": {
    "dev": "cross-env NODE_ENV=development concurrently \"npx tailwindcss -i ./docs/css/input.css -o ./docs/dist/output.css --watch\"  \"live-server ./docs\""
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "cross-env": "^5.2.0",
    "live-server": "^1.2.1",
    "tailwindcss": "^3.3.2"
  }
}
